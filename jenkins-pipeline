stage('checkout') {
    node {
        checkout scm
    }
}
stage('clean') {
    node {
        bat "mvn clean"
    }
}
stage('compile') {
    node {
        bat "mvn compile"
    }
}
stage('test') {
    node {
        bat "mvn test"
    }
}
stage('build') {
    node {
		bat "mvn install"
	}
}
